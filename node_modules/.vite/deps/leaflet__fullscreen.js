import {
  require_leaflet_src
} from "./chunk-C3P3YZLA.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/leaflet.fullscreen/dist/Control.FullScreen.js
var import_leaflet = __toESM(require_leaflet_src());
if (typeof document === "undefined") {
  console.warn('"window.document" is undefined; leaflet.fullscreen requires this object to access the DOM');
}
var nativeAPI = (() => {
  const methodMap = [
    // Standard
    [
      "requestFullscreen",
      "exitFullscreen",
      "fullscreenElement",
      "fullscreenEnabled",
      "fullscreenchange",
      "fullscreenerror"
    ],
    // New WebKit
    [
      "webkitRequestFullscreen",
      "webkitExitFullscreen",
      "webkitFullscreenElement",
      "webkitFullscreenEnabled",
      "webkitfullscreenchange",
      "webkitfullscreenerror"
    ]
  ];
  const baseList = methodMap[0];
  const ret = {};
  for (const methodList of methodMap) {
    if (methodList[1] in document) {
      for (let i = 0; i < methodList.length; i++) {
        ret[baseList[i]] = methodList[i];
      }
      return ret;
    }
  }
  return false;
})();
var eventNameMap = {
  change: nativeAPI.fullscreenchange,
  error: nativeAPI.fullscreenerror
};
var fullscreenAPI = {
  // Request fullscreen for a specific element
  async request(element, options) {
    element = element || document.documentElement;
    const requestMethod = nativeAPI.requestFullscreen;
    await this._callNative(() => {
      return element[requestMethod](options);
    });
  },
  // Exit fullscreen mode
  async exit() {
    if (!this.isFullscreen) {
      return;
    }
    const exitMethod = nativeAPI.exitFullscreen;
    await this._callNative(() => {
      return document[exitMethod]();
    });
  },
  // Helper to handle browser differences (Promise vs Event)
  async _callNative(action) {
    const result = action();
    if (result instanceof Promise) {
      await result;
      return;
    }
    await new Promise((resolve) => {
      const listener = () => {
        this.off("change", listener);
        resolve();
      };
      this.on("change", listener);
    });
  },
  // Add event listener for fullscreen changes
  on(event, callback) {
    const eventName = eventNameMap[event];
    if (eventName) {
      document.addEventListener(eventName, callback);
    }
  },
  // Remove event listener
  off(event, callback) {
    const eventName = eventNameMap[event];
    if (eventName) {
      document.removeEventListener(eventName, callback);
    }
  },
  nativeAPI
};
Object.defineProperties(fullscreenAPI, {
  isFullscreen: {
    get() {
      return Boolean(document[nativeAPI.fullscreenElement]);
    }
  },
  isEnabled: {
    enumerable: true,
    get() {
      return Boolean(document[nativeAPI.fullscreenEnabled]);
    }
  }
});
var FullScreen = import_leaflet.Control.extend({
  options: {
    position: "topleft",
    title: "Full Screen",
    titleCancel: "Exit Full Screen",
    forceSeparateButton: false,
    forcePseudoFullscreen: false,
    fullscreenElement: false
  },
  _screenfull: fullscreenAPI,
  onAdd(map) {
    let className = "leaflet-control-zoom-fullscreen";
    let container;
    let content = "";
    if (map.zoomControl && !this.options.forceSeparateButton) {
      container = map.zoomControl._container;
    } else {
      container = document.createElement("div");
      container.className = "leaflet-bar";
    }
    if (this.options.content) {
      content = this.options.content;
    } else {
      className += " leaflet-fullscreen-icon";
    }
    this._createButton(this.options.title, className, content, container, this.toggleFullScreen, this);
    this._map.fullscreenControl = this;
    this._map.on("enterFullscreen exitFullscreen", this._toggleState, this);
    return container;
  },
  onRemove() {
    import_leaflet.DomEvent.off(this.link, "click", import_leaflet.DomEvent.stop).off(this.link, "click", this.toggleFullScreen, this);
    if (this._screenfull.isEnabled) {
      import_leaflet.DomEvent.off(document, this._screenfull.nativeAPI.fullscreenchange, import_leaflet.DomEvent.stop).off(document, this._screenfull.nativeAPI.fullscreenchange, this._handleFullscreenChange, this);
    }
  },
  _createButton(title, className, content, container, fn, context) {
    this.link = document.createElement("a");
    this.link.className = className;
    container.appendChild(this.link);
    this.link.href = "#";
    this.link.title = title;
    this.link.innerHTML = content;
    this.link.setAttribute("role", "button");
    this.link.setAttribute("aria-label", title);
    import_leaflet.DomEvent.disableClickPropagation(container);
    import_leaflet.DomEvent.on(this.link, "click", import_leaflet.DomEvent.stop).on(this.link, "click", fn, context);
    if (this._screenfull.isEnabled) {
      import_leaflet.DomEvent.on(document, this._screenfull.nativeAPI.fullscreenchange, import_leaflet.DomEvent.stop).on(document, this._screenfull.nativeAPI.fullscreenchange, this._handleFullscreenChange, context);
    }
    return this.link;
  },
  async toggleFullScreen() {
    const map = this._map;
    const { fullscreenElement, forcePseudoFullscreen } = this.options;
    const container = fullscreenElement || map.getContainer();
    const useNative = this._screenfull.isEnabled && !forcePseudoFullscreen;
    if (map._isFullscreen) {
      map._exitFired = true;
      if (useNative) {
        await this._screenfull.exit();
      } else {
        container.classList.remove("leaflet-pseudo-fullscreen");
      }
      map.invalidateSize();
      map._isFullscreen = false;
      map.fire("exitFullscreen");
    } else {
      map._exitFired = false;
      if (useNative) {
        await this._screenfull.request(container);
      } else {
        container.classList.add("leaflet-pseudo-fullscreen");
      }
      map.invalidateSize();
      map._isFullscreen = true;
      map.fire("enterFullscreen");
    }
  },
  _toggleState() {
    const { title, titleCancel } = this.options;
    const isFullscreen = this._map._isFullscreen;
    this.link.title = isFullscreen ? titleCancel : title;
    this.link.setAttribute("aria-label", this.link.title);
    this.link.setAttribute("aria-pressed", isFullscreen.toString());
    this.link.classList.toggle("leaflet-fullscreen-on", isFullscreen);
  },
  _handleFullscreenChange() {
    const map = this._map;
    const wasExitedExternally = !this._screenfull.isFullscreen && map._isFullscreen && !map._exitFired;
    if (wasExitedExternally) {
      map._exitFired = true;
      map._isFullscreen = false;
      map.fire("exitFullscreen");
      map.invalidateSize();
    }
  }
});
import_leaflet.Map.include({
  toggleFullscreen() {
    this.fullscreenControl.toggleFullScreen();
  }
});
export {
  FullScreen,
  FullScreen as default
};
/*! Bundled license information:

leaflet.fullscreen/dist/Control.FullScreen.js:
  (*!
   * leaflet.fullscreen
   * (c) Bruno B.; MIT License
   * https://github.com/brunob/leaflet.fullscreen
   *)
  (*! GENERATED FILE - DO NOT EDIT DIRECTLY. Edit files in src/ and run 'npm run build' *)
*/
//# sourceMappingURL=leaflet__fullscreen.js.map
